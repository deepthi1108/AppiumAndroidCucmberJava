package setUp;

import org.junit.runner.RunWith;
import cucumber.api.CucumberOptions;
import cucumber.api.junit.Cucumber;
 
@RunWith(Cucumber.class)
@CucumberOptions(
		 features = "src/test/java/features/anonymousUserPlaceOrder.feature",
		 glue = {"setUp","step_definitions"},
		 tags = {"@test"}
	       
	        )
	
public class runner {
 
}
